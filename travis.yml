language: node_js
node_js: node
services:
- mongodb
addons:
  apt:
    sources:
    - mongodb-3.2-precise
    packages:
    - mongodb-org-server
deploy:
  provider: heroku
  app: mjm-shopping-list
  on:
    repo: elmeltone/mjm-shopping-list
